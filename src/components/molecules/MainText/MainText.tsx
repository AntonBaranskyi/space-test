import { TextWrapper } from './MainText.styled';
import { MainTitle } from '../../atoms/MainTitle';

export const MainText = () => {
  return (
    <TextWrapper>
      <MainTitle />
    </TextWrapper>
  );
};
