import styled from 'styled-components';

export const BottomMainWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;

  cursor: pointer;

  position: absolute;

  bottom: 33px;

  left: 50%;
  transform: translateX(-50%);
`;
